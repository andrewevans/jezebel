<ul class="list-unstyled">
  {{#each sorted_characters as |character|}}
      <li>{{character.title}} <sup>{{character.weight_ranked}}</sup></li>
  {{/each}}
</ul>

<h1>Your inception</h1>
{{characters-list/animated-list characters=filtered_characters}}