<h1>{{user.username}}'s palette</h1>

{{#each user.palettes as |palette|}}
  <h2>{{palette.title}}</h2>

  {{#each palette.shades as |shade|}}
    <div><button class="btn btn-default btn-lg btn-block handle">{{shade.value}}</button></div>
  {{/each}}
{{/each}}